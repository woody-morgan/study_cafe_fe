import { Variants } from 'framer-motion';

export const HorizontalItemListVars: Variants = {
  initial: {
    transition: {
      duration: 0,
    },
  },
  animate: {
    scale: 1.1,
    transition: {
      delay: 0,
    },
  },
};
